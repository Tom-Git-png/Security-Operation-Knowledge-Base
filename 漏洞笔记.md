文件漏洞类型：类型一。.jpg后缀文件改为.php，绕过.php后缀认证（或禁用JS加载）。类型二。content-type 上传类型。更改 content\_type 类型。类型三。.php后缀文件改为.php3。类型四。配置文件 .htaccess  php解释器。将php文件当做jpg文件执行。类型五。文件后缀是否区分大小写。类型六。文件后缀是否去空格。类型七。文件后缀是否加点。类型八。文件后缀加上 ::$DATA（默认存在）。用完后在浏览器地址中删除（默认存在）。类型九。利用去点的php代码漏斗。类型十。使用.pphphp文件后缀。类型十一。%00 代表截断 URL不会解析后面的编码。更改请求 GET。.php 文件后缀改为 .jpg 文件后缀。将 .jpg文件 以 .php保存 保存。类型十二。更改 POST 请求。将 %00 转换为 URL 编码。类型十三。做图片马（png格式图片中加入php文件）。php 语言中 include 为导入 文件 功能。文件包含漏洞：&x20;<?php/\*本页面存在文件包含漏洞，用于测试图片马是否能正常运行！\*/header("Content-Type:text/html;charset=utf-8");$file = $\_GET\['file'];if(isset($file)){&x20;   include $file;}else{&x20;   show\_source(\_\_file\_\_);}?>copy 090805.png /b + php.php /a webshell.png。类型十六。与类型十三操作类似。类型十七。将请求发送到 intruder。利用 Burpsuite 向目标地址上传文件，在来不及删除前访问文件。类型十八。与类型十七类似。在 .php 后面加上 .7z 后缀。类型十九。将文件类型加入 .php 文件后缀 并用 URL 编码转换。类型二十。更改 content-type 形式。利用 数组与索引 形式拼可上传格式。00 截断后面的文件后缀。reset 取出数组第一个值。LFI 本地文件包含。RFI 远程文件包含。XSS 跨站脚本攻击：反射型 存储型 DOM型。反射型：将脚本嵌入到URL中。1).弹窗攻击：通过 js 代码校检。2).修改页面内容：document 整个文档的顶部 拿取信息。innerHTML 网页 body 内文字。3).窃取 cookie 信息。4).POST请求。存储型：将脚本存储在服务器上。1).xss 盲打 将脚本输入进入网站。2).DOM型：修改DOM结构执行恶意脚本。3).javascript:alert( ) 伪协议漏洞。4).onclick 点击代码。扩展类型：1.img onerror 代码。RCE漏洞：远程代码执行和远程代码命令执行。1).eval,assert 将传入的代码当做 PHP 代码执行。2).攻击靶场 system,shell\_exec,exec,passthru 执行命令并投到屏幕上。A I B ：执行后面语句。A II B ：前面执行失败，执行后面的语句。A & B：都执行。A && B： A 为假的时候执行 B。gzinflate 解压数据。扩展类型：读取文件  cat flag ; cat fl; cat ?la\* ; ca\\t  fl\\ag  \\为转义符 ; cat fl''ag ; ca$\*t fl$\*agcurl ; strings ; bash -v ; rev。漏洞挖掘：白盒：看代码 代码审计。eval（ ）;system（ ）;shell\_exec（ ）;CSRF漏洞：跨站请求伪造：利用已有登录状态。发送伪造链接。由受害者点击。1.GET 请求型 CSRF（最简单）GET 请求的参数直接放在 URL 上。黑客把恶意 URL 放到图片、超链接里。当用户加载这个资源，浏览器自动发起请求，自动带上对应站点 Cookie。示例：修改用户邮箱接口：GET /change_email?email=hacker@test.com HTTP/1.1恶意页面代码：<!-- 隐藏图片，用户打开页面就自动发起请求 --><img src="http://target.com/change_email?email=hacker@test.com" width="0" height="0">2.POST 请求型 CSRF。GET 不行时，构造自动提交的表单，用 JS 自动提交 POST 请求。恶意页面示例：<form action="http://target.com/change_pwd" method="POST" id="csrfform">&x20;   <input type="hidden" name="password" value="123456"></form><script>&x20;   document.getElementById("csrfform").submit();3.基于链接诱导（点击劫持 + CSRF）。把恶意链接放在钓鱼页面、QQ / 微信群、邮件里，骗用户点击。<a href="http://target.com/change?money=1000">点击领取红包</a>4.JSON‑POST CSRF（常见坑）。后端接收 Content‑Type:application/json。如果后端没有校验 Origin/Referer，利用 <iframe> + JS 跨站发送 fetch 请求。5. 引用型 CSRF（图片、CSS、JS、iframe）。<img src="恶意url"><script src="恶意url"><iframe src="恶意url">这些标签可以发起跨域 GET 请求，自动携带 Cookie。SSRF漏洞：服务器端请求伪造。服务器内有伪造链接，地址。curl _exec： 将得到的地址放在浏览器 URL 后。url = file：命令 获取信息 dict:// 请求地址。SQL注入：代码注入技术，在输入字段中输入恶意代码。危害：数据泄露，数据篡改，拒绝服务（DOS），账户劫持，绕过安全措施，扩展攻击面。1.union 注入：通过 SQL 注入漏洞，获取所有用户信息。逻辑运算符优先级 and>or>not。or 左右两边有一个为真就位真 例如：or '1' = '1' 默认在输入后的信息加引号。拿取用户信息。2.1）.查询库的信息：使后面的条件成立 or '1' = '1'。关系：用户名>库>表>字段>数据。union 合并 select database( ),'','',''; 有几列添加几列。select database 。拿取库的信息。2）.查询所有表的信息：select *from information_schema.tables;select Table_Name from information_schema.TABLES where Table_schema='库名'；group_concat( )：多个字符串变为一个字符串。select group_concat(Table_Name) from information_schema.TABLES where Table_schema='库名'；拿去表的信息。3）.查询字段信息：column。select *from information_schema.columns;select Column_Name from information_schema.columns where Table_name='表名'；select group_concat(Column_Name) from information_schema.Columns where Table_schema='表名'；4）.查询数据：select database( ):逻辑漏洞：水平越权（平行越权）：同权限用户之间访问别人数据。A 用户和 B 用户都是普通用户；A 可以查看 / 修改 B 的数据。例子：接口：`/getorder?order_id=1001`。自己账号：order_id=1001 是我的订单；Burp 抓包把`order_id=1002`（别人订单 ID），后端没有校验这个订单是不是属于当前登录用户，直接返回别人订单、手机号、收货地址。测试：注册 2 个账号 A、B；拿 B 的资源 ID，在 A 账号下调用接口；看是否返回 B 数据。垂直越权：低权限→高权限。普通用户，直接访问管理员接口；普通用户拿到管理员操作。例子：普通用户访问后台地址 `/admin/userlist`；或者请求包 `role=user`改成`role=admin`；后端信任前端传的 role 参数，直接赋予管理员权限。区分记忆：- 水平越权：平级，用户 A 看用户 B 的数据。- 垂直越权：升级，普通用户干管理员的活。未授权访问：不登录直接访问需要登录的接口。不携带 Cookie/Session，直接访问接口，返回数据；完全跳过登录。账户模块逻辑漏洞（注册、登录、找回密码）。1.验证码逻辑漏洞。- 验证码返回响应包中，直接拿到验证码；- 验证码不校验手机号，改手机号参数就能重置任意账号密码；- 验证码可暴力爆破；- 修改响应包状态码绕过验证码校验（响应包`status=0`改成`status=1`）。2. 短信轰炸：没有频率限制，重放验证码请求，无限给手机号发短信。3. 密码重置任意用户漏洞：修改请求里面手机号 /userid，重置其他人密码，中高危，赏金高。支付 / 优惠逻辑漏洞（高危，资金风险）。1. 篡改金额：下单抓包把`amount=99`改成`amount=0`、`amount=-100`，后端没校验，0 元下单、甚至余额增加。 真实 SRC 测试严禁真实下单，只用自己测试账号，不能产生真实交易。2. 优惠券：单人限领 1 张，并发请求同时发送，一次性领取多张优惠券（竞争条件 / 并发漏洞）；优惠券过期还能使用；一张券重复多次使用。3. 订单数量改为负数，实现金额倒扣。流程绕过漏洞。业务是多步骤：A→B→C→D；攻击者跳过中间步骤直接访问后面接口。例子：正常流程：提交资料→短信验证→实名认证。直接抓包跳过短信验证接口，调用实名认证接口，直接完成实名。并发 / 竞争条件漏洞。 多个请求同时并发提交，后端没有加锁，判断逻辑出现时间差。例子：优惠券限制每人 1 张；Burp 同时发送 20 次领取请求；同一账号拿到 20 张券。数据库判断库存的时候还没来得及更新。SRC 挖掘逻辑漏洞通用思路（实操）。1. 梳理完整业务流程，注册 2‑3 个测试账号；2. 抓包 Burp Suite，观察所有参数：id、user_id、order_id、amount、role、is_check、phone；3. 修改参数：改为别人 ID、0、负数、空值；删除参数；4. 重放请求；跳过流程步骤；并发多请求；5. 观察返回是否出现业务异常结果。重点：后端不要信任前端传过来的任何参数，所有关键逻辑必须服务端校验。前端页面限制都可以抓包绕过。