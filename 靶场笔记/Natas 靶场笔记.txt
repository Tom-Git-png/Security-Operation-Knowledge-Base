Natas关卡:0-16关：

关卡：

0:密码在源代码里。

1:密码在源代码里。

2：通过添加 URL /file 获取。

3：通过添加 URL /robots.txt 获取。

4：通过抓包修改 Referer 请求头。

5：通过修改 Cookie 登录信息。

6：通过已有脚本中的信息，找到这一关卡的 secret ，提交密码，得到 Flag。

7：通过修改 URL 中的参数获得 Flag。

8：利用脚本解码。

9：在 URL 中输入正确的指令。

index.php?needle=【你的注入payload】。

10：利用 grep 搜索格式绕过限制。

11：利用已知的默认明文和网站发给你的加密 Cookie 密文，逆向算出 XOR 密钥；拿到密钥后自己伪造一份开启密码的加密 Cookie。

12：更改要解析的后。

13：更改文件名 木马格式 GIF89a 图片格式。

14：绕过用户密码审核。

"  → 英文双引号，用来闭合SQL里 username=" 前面的引号。
or 1=1  → 永真条件，让查询永远成立。
\--  → MySQL单行注释，把后面剩下的SQL全部废掉。
Post 中 URL 可以用 + %20 替换掉空格。
GET 中只能用 %20。

15：SQL 布尔盲注入脚本。

16：运用 Shell\_盲注爆破脚本。

17：延时盲注爆破脚本。